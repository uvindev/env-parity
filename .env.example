# Optional aggregate event delivery. Source and environment data are never event properties.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Optional real Team checkout URL. The default action opens a pilot email.
NEXT_PUBLIC_TEAM_URL=
